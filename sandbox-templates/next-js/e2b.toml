# This is a config for E2B sandbox template.
# You can use template ID (czi3esc8zi09dm0ak5z0) or template name (We-build) to create a sandbox:

# Python SDK
# from e2b import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("We-build") # Sync sandbox
# sandbox = await AsyncSandbox.create("We-build") # Async sandbox

# JS SDK
# import { Sandbox } from 'e2b'
# const sandbox = await Sandbox.create('We-build')

team_id = "4a3d3457-fa1e-4b47-95d0-0ac95208266a"
start_cmd = "/compile_page.sh"
dockerfile = "e2b.Dockerfile"
template_name = "We-build"
template_id = "czi3esc8zi09dm0ak5z0"
